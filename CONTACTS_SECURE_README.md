# 安全通讯录系统

这是一个现代化、安全的通讯录管理系统，用于替代旧版的 contacts.html。

## 主要改进

### 安全性提升
- ❌ **旧版问题**：密码硬编码在前端，131位干部信息明文暴露
- ✅ **新版方案**：基于 Manus OAuth 身份验证，数据加密存储在服务器端

### 技术架构
- **前端**：React 19 + Tailwind CSS 4
- **后端**：Express + tRPC
- **数据库**：MySQL
- **认证**：Manus OAuth

### 核心功能
- 用户身份验证（基于 Manus OAuth）
- 联系人增删改查
- 按姓名、电话搜索
- 按部门筛选
- 紧急联系电话展示
- 管理员权限控制

## 部署说明

本系统需要服务器端环境运行，不能像旧版 contacts.html 那样直接在浏览器中打开。

### 部署方式

1. **使用 Manus 平台部署**（推荐）
   - 项目已在 Manus 平台创建
   - 点击"Publish"按钮即可发布到 manus.space 域名
   - 支持自定义域名绑定

2. **自行部署到服务器**
   - 需要 Node.js 22+ 环境
   - 需要 MySQL 数据库
   - 配置环境变量（数据库连接、OAuth 配置等）
   - 运行 `pnpm install && pnpm build && pnpm start`

## 数据迁移

如需从旧版 contacts.html 导入数据，请联系管理员使用数据导入工具。

## 安全注意事项

- 所有敏感数据已从前端代码中移除
- 使用 HTTPS 加密传输
- 实施了基于角色的访问控制
- 定期备份数据库

## 联系方式

如有问题，请联系系统管理员。

---

**注意**：旧版 contacts.html 已保留在仓库中作为备份，但不建议继续使用，因为存在严重的安全隐患。
